# Put sterling-specific config in this file, which overrides the defaults in values.yaml

ingress:
  hosts:
    - host: neurobridges.org
      paths:
        - path: /
  tls:
    - secretName: neurobridges.org-tls
      hosts:
        - neurobridges.org
